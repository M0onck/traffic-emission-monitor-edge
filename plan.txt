traffic-emission-monitor/
├── ✅ bootstrap.py                  # [已完成] 替换感知组件，恢复双模型管线。
├── 🟡 config.json                   # [待修改] 需增加 HEF_PATH、POST_SO_PATH 等边缘端特有字段。
├── domain/                          # 🟢 [保留不变] 领域驱动层全部保留。
├── infra/
│   ├── ⚪ concurrency/ocr_worker.py # [已废弃] 完全删除，由主线程的小图直接跑 ONNX 替代。
│   ├── 🟡 config/loader.py          # [待修改] 配合 config.json，新增特有字段的解析读取。
│   ├── 🟢 store/                    # [保留不变] 数据库逻辑保留。
│   └── 🟡 sys/process_optimizer.py  # [待修改] 需重构以绑定树莓派 5 的 4 个 Cortex-A76 核心。
├── perception/
│   ├── ✅ gst_pipeline.py           # [已完成] 新建，作为底层车流抓取的发动机 (代替原 camera.py)。
│   ├── ✅ kinematics_estimator.py   # [已完成] 剔除了卡尔曼滤波，降级为轻量级坐标差分。
│   ├── ⚪ math/kalman_filter.py     # [已废弃] 硬件已包含过滤机制，直接删除。
│   ├── 🟢 math/geometry.py          # [保留不变] 透视变换工具保留。
│   ├── ⚪ lpr_adaptor.py            # [已废弃] 分类逻辑已打通，此中间件可删除。
│   ├── plate_classifier/            # 🟡 推理核心目录 (待最后修补)
│   │   ├── 🟢 core/multitask_detect.py  # [撤销重构，保留不变] 恢复保留！需要在 Python 端抓车牌四个角点。
│   │   ├── 🟢 core/classification.py    # [保留不变] 依然用于颜色分类。
│   │   ├── 🟢 core/tools_process.py     # [保留不变] 透视变换所需。
│   │   └── 🟡 pipeline.py               # [待修改] 替换为我在上一次对话中发给你的修正版 `EdgePlateClassifierPipeline`。
│   └── ⚪ cpp_postprocess/             # [方案取消！] 直接使用 Hailo 官方 yolov8 的 .so 库即可，不用自己写 C++！
├── ✅ app/monitor_engine.py         # [已完成] 改为 appsink 被动接收模式，并修正了传入车辆裁剪小图的逻辑。
└── ui/
    ├── 🟢 calibration_window.py     # [保留不变] 
    ├── 🟢 console_reporter.py       # [保留不变] 
    └── 🟢 renderer.py               # [好消息：保留不变] 因为数据结构对齐了，可视化层可以直接复用。
